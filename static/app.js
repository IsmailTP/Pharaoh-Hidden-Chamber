// ancient encoding system
function decodeGlyph(g) {
    // Only the worthy shall decode the glyphs of the temple
    return atob(g);
}

// Truth is never seen… it is revealed when transformed.
console.log("Welcome to the Temple of Khafra, Explorer.");
